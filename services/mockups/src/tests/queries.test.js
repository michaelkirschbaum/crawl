var queries = require('../db/queries')
var supertest = require('supertest')

describe('mockups', () => {
  it('should', () => {

  })

  it('should', () => {

  })

  it('should', () => {
    
  })
})
